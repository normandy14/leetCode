#include <iostream>
#include <string>
#include <vector>

using namespace std;

int main() {
  string s = "flow";
  string matchStr = s.substr(0, s.size());
  cout << matchStr << endl;
  int i = 0;
  int j = s.size();
  vector<string> v;
  for (int i=0)
  return 0;
}